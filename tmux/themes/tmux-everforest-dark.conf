# The option must be set BEFORE running the plugin: everforest-theme.tmux reads
# @tmux-everforest and falls back to its own dark-medium default when unset.
set -g @tmux-everforest 'dark-medium'
run ~/.tmux/plugins/tmux-everforest/everforest-theme.tmux
